<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.crave.food.csse_android_app">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.Login"></activity>
        <activity android:name=".activities.SupplierRegistration"></activity>
        <activity android:name=".OrderViewSitemanager" ></activity>
        <activity android:name=".OrderPlaceSitemanager">
        </activity>
        <activity android:name=".OrderViewSupplier"></activity>
        <activity android:name=".OrderRespondSupplier"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>